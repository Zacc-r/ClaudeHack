# Agents

Agent policies, runbooks, and specs for Cursor, Claude, OpenClaw.

- `policies/` — AI write policy, agent guardrails (see `AI_WRITE_POLICY.md`)
- `openclaw/` — OpenClaw agent specs and run templates
- `runbooks/` — Agent runbooks and setup guides
- `skills/` — Agent skills and workflows
